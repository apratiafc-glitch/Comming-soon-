deployment:
  tasks:
    - export DEPLOYPATH=/home/apratifoodscom/public_html/apratifoods.asia/
    - /bin/mkdir -p $DEPLOYPATH
    # Copy Frontend (Nuxt static files, includes dotfiles like .htaccess)
    - /bin/cp -rf .output/public/. $DEPLOYPATH/
